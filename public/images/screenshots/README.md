# Screenshot assets

The `sprintmaths-*.png` files are neutral copies used by current Bac 2027 pages.

The `bac-2026-*.png` files are intentionally kept because `/bac-maths-terminale-2026` still renders `Bac2026ProductDemo`, where the 2026-specific filenames remain coherent and preserve existing URLs.
